# What actually earns replies

<!-- GENERATED by tools/reply_features.py — DO NOT EDIT BY HAND. -->
<!-- Hand edits are overwritten on the next run. Human judgement calls -->
<!-- belong in config/voice.md, which outranks this file. -->

Regenerated 2026-07-31. **The cold book is too young to have taught us anything yet** — 1 cold 1:1 email has come of age, with 88 more still awaiting a verdict. Nothing below is a rule yet.

## Rules — apply these when drafting (SKILL.md Step 7)

**None yet.** Nothing has cleared the evidence bar, which at this corpus size is the honest answer rather than a failure. Draft to `config/voice.md` as usual.

This section fills itself in as batches land. It stays empty rather than shipping a rule built on four replies, because a false rule propagates into every future email and is far more expensive than no rule at all.

## In flight — what the current batch will be able to answer

88 email(s) sent within the last 3 days. Silence from them means nothing yet.

These comparisons have both arms populated, so they will resolve once the batch matures:

- `open_names_them` — yes (47), no (41)
- `subj_names_company` — no (53), yes (35)
- `subj_names_finley` — no (68), yes (20)

Never varied, so unanswerable from this batch: `greeting`, `open_question`.

Body text is not banked for most of this batch, so these stay untestable until it is: `body_paras`, `body_words`, `cta_minutes`, `cta_shape`, `dental_angle`, `drive_time`, `em_dashes`, `mentions_tiers`, `names_days`, `three_options`, `you_we_ratio`.

## Watching — not proven, do not act on yet


## Targeting, not writing

These describe who answers, not how to write to them. They belong in `config/autopilot.md`'s rotation, not in a draft.


---

Corpus: 0 of 1 emails have full bodies banked in `tracker/email-corpus.jsonl`; the rest contribute subject, opening line and timing only. Body-level rules get sharper as that number climbs.
